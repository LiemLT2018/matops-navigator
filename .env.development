# Dev: leave empty to use Vite proxy → MatOps.Api.Host http://localhost:5145 (see vite.config.ts).
# Set full URL to bypass proxy, e.g. VITE_API_URL=http://localhost:5145
# MatOps.Tenant.Api local default is http://localhost:5079 (change vite proxy target if you use Tenant).
VITE_API_URL=
